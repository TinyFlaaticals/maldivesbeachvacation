<% content_for :title, "Editing room category" %>

<h1>Editing room category</h1>

<%= render "form", room_category: @room_category, property: @property, url: admin_property_room_category_path(@property, @room_category) %>

<br>

<div>
    <%= link_to "Show this room category", admin_property_room_category_path(@property, @room_category) %> |
    <%= link_to "Back to room categories", admin_property_room_categories_path(@property) %>
</div>