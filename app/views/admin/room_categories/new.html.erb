<% content_for :title, "New room category" %>

<h1>New room category</h1>

<%= render "form", room_category: @room_category, property: @property, url: admin_property_room_categories_path(@property) %>

<br>

<div>
    <%= link_to "Back to room categories", admin_property_room_categories_path(@property) %>
</div>