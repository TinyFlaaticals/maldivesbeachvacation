<div id="<%= dom_id hotel %>" class="bg-white rounded-lg shadow-md overflow-hidden">
  <div class="h-48 bg-gray-300 flex items-center justify-center">
    <%# <svg class="h-12 w-12 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z" />
    </svg> %>
    <% if hotel.images.attached? %>
      <%= image_tag hotel.images.first, class: "h-full w-full object-cover" %>
    <% else %>
      <div class="h-full w-full bg-gray-300 flex items-center justify-center">
        <p class="text-gray-400">No image</p>
      </div>
    <% end %>
  </div>
  
  <div class="p-4">
    <h3 class="text-xl font-semibold mb-2"><%= hotel.name %></h3>
    
    <div class="flex items-center mb-2">
      <span class="text-yellow-500">
        <%= "★" * hotel.rating.to_i %>
      </span>
      <span class="text-gray-600 ml-1">(<%= hotel.rating %>)</span>
    </div>


    <div class="text-sm text-gray-500">
      <p class="mb-1">
        <span class="font-medium">Location:</span>
        <%= hotel.latitude %>, <%= hotel.longitude %>
      </p>

      <%= link_to "View", admin_hotel_path(hotel), class: "rounded-md bg-indigo-50 px-3 py-2 text-sm font-semibold text-indigo-600 shadow-sm hover:bg-indigo-100" %>
    </div>
  </div>
</div>
